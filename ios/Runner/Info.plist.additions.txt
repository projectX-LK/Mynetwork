<!--
  Add these keys into ios/Runner/Info.plist (inside the outer <dict>).
  This is a snippet to merge in, not a full replacement file, since the
  rest of Info.plist is generated by `flutter create` and varies by
  Flutter/Xcode version.
-->
<key>NSCameraUsageDescription</key>
<string>LocalCast uses the camera to scan a pairing QR code from another device.</string>

<key>NSLocalNetworkUsageDescription</key>
<string>LocalCast connects to other devices on your WiFi network to share and stream media.</string>

<key>NSBonjourServices</key>
<array>
    <string>_localcast._tcp</string>
</array>

<!--
  Known iOS limitation (see README): AVPlayer, used internally by the
  video_player plugin, performs its own TLS trust evaluation for any
  HTTPS URL and does not consult app-level certificate pinning. This app
  avoids the problem entirely by only ever pointing AVPlayer/streaming
  players at a plain http://127.0.0.1 loopback URL served by the app's own
  relay (see lib/services/relay_server.dart), so no ATS exception for
  arbitrary HTTPS is required. If you extend this app to point a native
  player directly at a remote HTTPS host, you would need an ATS exception
  here and would lose certificate pinning for that connection.
-->
